<h1>New Topic</h1>

<%= form_for @changeset, topic_path(@conn, :create), fn f -> %>
  <div>
    <%= text_input f, :title, placeholder: "Title"  %>
    <%= error_tag f, :title  %>
  </div>

  <div>
    <%= submit "Save Topic" %>
  </div>
<% end %>
